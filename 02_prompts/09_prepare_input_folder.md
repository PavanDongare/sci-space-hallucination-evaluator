You are preparing one SciSpace run folder for a hallucination evaluator.

The evaluator works best when the folder contains exactly these canonical files:

```text
user_query.txt
search_queries.txt
search_queries.json
intermediate_report.md
final_report.md
```

Your task:

1. Inspect the messy input folder.
2. Identify the original user query.
3. Identify the search-query log generated by SciSpace.
4. Identify the intermediate insights/extraction report.
5. Identify the final report with citations and references.
6. Produce the canonical file contents.

Do not rewrite or summarize the actual content. Copy the relevant source content as-is into the canonical destination file.

How to identify files:

- `user_query.txt`: short user request, usually one paragraph.
- `search_queries.txt`: original messy log containing database searches such as "Searched SciSpace", "Searched Scispace Full Text", "Searched Google Scholar", "Searched PubMed", paper counts, and status messages.
- `search_queries.json`: cleaned channel/query pairs extracted from `search_queries.txt`.
- `intermediate_report.md`: insights, extracted findings, TL;DR, or summary generated before the final report.
- `final_report.md`: full structured report with inline citations like `[1]` and a `References` section.

Important search-channel nuance for later evaluation:

- SciSpace semantic search may be natural language because it searches abstracts/summaries.
- SciSpace full-text search may be keyword-heavy because it searches full PDFs with deterministic text matching.
- Google Scholar may use Boolean syntax.
- PubMed may use MeSH terms and Boolean syntax.

Return only this JSON manifest:

```json
{
  "user_query.txt": "source filename",
  "search_queries.txt": "source filename",
  "search_queries.json": [
    {
      "channel": "SciSpace",
      "query": "exact query string"
    }
  ],
  "intermediate_report.md": "source filename",
  "final_report.md": "source filename",
  "ignored_files": ["filename"]
}
```

After producing this manifest:

1. Copy the source contents into `user_query.txt`, `search_queries.txt`, `intermediate_report.md`, and `final_report.md`.
2. Write the cleaned query array into `search_queries.json`.
3. Do not rename the original messy files unless explicitly asked.
